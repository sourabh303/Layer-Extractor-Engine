{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BootRequest",
  "type": "object",
  "properties": {
    "jwt": {
      "type": "string",
      "description": "The Supabase JWT to verify for boot authorization."
    },
    "machine_id": {
      "type": "string",
      "description": "The unique hardware identifier of the machine."
    }
  },
  "required": ["jwt", "machine_id"]
}
